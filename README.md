# route53

Script to create DNS records in AWS Route53.

`route53-create-record.sh` — interactive prompt to create A, CNAME, or MX records in the first hosted zone of the account. Requires the AWS CLI configured with appropriate credentials.
